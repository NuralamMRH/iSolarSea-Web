"Dont's use this next/router in this project"
"Use react-router-dom instead of next/router"
"Use PostgreSQL instead of MongoDB"
"Every form should have a validation"
"After form submit, show a success message"
"Use Supabase for database"
"Form data need to submit in Supabase database"
"If you use Supabase, you need to create a table for this project"
"Use TypeScript instead of JavaScript"
"Use TailwindCSS instead of CSS"
"Use NextJS instead of ReactJs"
"This project make with Vite and NextJS"
"In this project, After login or Registration then when a form submit then need to user id is required"
"Don't run db push --include-all"
"If you want to run push then just run for latest table not for all"
"Because sometime this command can remove my all data from the supabase"
